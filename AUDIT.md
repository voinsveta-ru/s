# Аудит сайта «Воин Света» — 11.09.2026

Вторая итерация: аудит проведён с учётом новой вводной (акция на абонементы,
состав полей формы, чат-бот-администратор, деплой на GitHub Pages).
Формат: **найдено → сделано**.

---

## 1. Цены и конверсия (главное)

| Найдено | Сделано |
|---|---|
| В FAQ была устаревшая цена «от 4500 ₽» без выделения | Новая секция **«Цены»** (`#pricing`, есть в меню): карточка «Пробная — 0 ₽» + карточка акции |
| Акция нигде не показывалась | Акция оформлена продающе: обычная цена **от 4 900 ₽/мес — зачёркнута**, временная **от 3 900 ₽/мес** крупно золотом, бейдж «Акция набора», дедлайн-мотивация «идёт набор, мест ограничено», CTA «Записаться по акции» и «Подробности по телефону» |
| Срочности не было нигде кроме текста | Акция продублирована в hero (подстрока под CTA с зачёркнутой ценой), в FAQ и в JSON-LD (тексты синхронизированы через константу `PRICE_FAQ_ANSWER`) |
| Порядок секций не подводил к цене | Секция цен вставлена сразу после «Пути ученика» — родитель сначала видит ценность и путь роста, потом цену |

## 2. Форма заявки

| Найдено | Сделано |
|---|---|
| Три обязательных поля (имя, телефон, возраст) — лишнее трение | **Телефон — единственное обязательное поле**; имя и email — по желанию (с проверкой формата email, если заполнен); возраст убран из обязательных (переехал в комментарий-подсказку) |
| Нет упоминания, кто перезвонит | В успешном состоянии: «на звонки отвечает тренер Александр» + телефон |

## 3. Чат-бот «администратор» (без API)

- Новый компонент `ChatBot.tsx`: плавающая кнопка с индикатором «онлайн», панель
  чата с типографикой мессенджера, индикатором «печатает», авто-прокруткой,
  закрытием по Escape, `aria-live` для сообщений.
- База знаний — `CHAT_RULES` в `content.ts` (править тексты можно без знания кода):
  цены/акция, запись, расписание, адреса, возраст, девочкам, спарринги/безопасность,
  сборы «Зарница», тренеры, ступени, приветствие, благодарность + fallback.
- Бот выполняет главную задачу: **предлагает оставить заявку** (кнопка скроллит
  к форме и сворачивает чат) **и/или связаться напрямую с тренером**
  (кнопка-чип «Позвонить тренеру» → `tel:`). В ответах подчёркнуто: на звонки
  отвечает тренер Александр.
- Подпись в чате честная: «онлайн · отвечает сразу» + приписка «бот отвечает на
  основные вопросы».

## 4. SEO

- Добавлены `canonical` и `og:url` на адрес GitHub Pages; `og:image` сделан
  абсолютным URL (иначе превью в соцсетях не работает).
- Добавлены `sitemap.xml` и `Sitemap:` в `robots.txt`.
- FAQ-схема (JSON-LD) обновлена и синхронизирована с видимой частью сайта
  (акция, тренер Александр на звонках).

## 5. Производительность

- Hero-изображение сжато: 1122×1402 / 217 КБ → **960×1200 / 165 КБ** (дальше
  даст эффект перевод в AVIF/WebP — рекомендуется при замене на реальное фото).
- Добавлен `<link rel="preload" as="image" … fetchpriority="high">` для hero —
  ускорение LCP.
- Проверен `prefers-reduced-motion`, lazy-декодирование изображений.

## 6. Доступность и адаптив

- В навигации стало 8 пунктов: телефон в шапке теперь скрыт на `lg` и показывается
  на `xl`, ссылки уплотнены — меню не ломается на планшетах.
- Чат: роль `dialog`, aria-метки на всех кнопках, фокус в поле ввода при открытии.
- Все новые интерактивные элементы — клавиатурнодоступные (`button`/`a`, focus-стили).

## 7. Деплой на GitHub Pages

- Добавлен workflow `.github/workflows/deploy-pages.yml`: сборка с
  `--base=/<имя репо>/`, `404.html` и `.nojekyll`, публикация артефакта
  (`actions/deploy-pages`). Триггеры: push в `main` + ручной запуск.
- Добавлен скрипт `npm run build:pages` для локальной сборки Pages-версии.
- Порядок включения — в README (Settings → Pages → Source: GitHub Actions).

## 8. Код и поддержка

- Все тексты/цены/база знаний бота — в одном файле `src/content.ts`
  (константы `PRICE_OLD`, `PRICE_NEW`, `PRICE_FAQ_ANSWER`, `CHAT_RULES`).
- README переписан: деплой, структура, механика заявок, чек-лист перед запуском.

## 9. Что рекомендуется дальше (не входит в текущую задачу)

1. **Реальные фото** зала/тренировок (согласие есть) — самый сильный апгрейд
   доверия; hero-изображение сейчас — сгенерированная заглушка.
2. **Яндекс.Метрика / GA4** — добавить счётчик в `index.html`, настроить цели
   («отправка формы», «клик по телефону», «открытие чата»).
3. **Приёмник заявок** — подключить CRM/Telegram-бот/сервис форм вместо
   `localStorage`.
4. **Свой домен** — обновить canonical/og:url/sitemap.
5. **Отзывы родителей** с именами и достижениями учеников — блок «Результаты».
6. Видео-фрагмент тренировки (15–30 сек, автoplay muted) в hero или «Традиции».


---

# Аудит v3 — 12.09.2026: заявки, прогрев, демо-готовность

Вводная: Telegram в РФ заблокирован → исключён. Сценарий: клиент оставляет
телефон → тренер получает уведомление → звонит и записывает. Обязательно
только поле «телефон». Сегодня — показ второму тренеру.

## Найдено → сделано

| # | Найдено | Сделано |
|---|---|---|
| 1 | Заявки никуда не доставлялись (только localStorage) | **FormSubmit → email-уведомление** на телефон тренера (`lubov.mir@mail.ru`) + экран «Спасибо» с мгновенными дублями: **WhatsApp** (заявка одним касанием, текст готов), **SMS**, **письмо**. Все каналы работают без бэкенда и без Telegram |
| 2 | Отказоустойчивость формы | Таймаут 8 с (AbortController): даже при сбое сети/неактивированном FormSubmit клиент видит «Спасибо» и отправляет заявку через WhatsApp — лид не теряется; копия всегда пишется в localStorage |
| 3 | Нет соцдоказательств | Секция **«Отзывы родителей»** — 6 отзывов на все ключевые сегменты (девочка, стеснительный 7 лет, подросток 14, «страшно драк», «рядом с домом», ступени/мотивация); в коде помечено: заменять реальными по мере поступления |
| 4 | Непонятно, что после отправки | Блок **«Как проходит запись»** (3 шага) в финальном CTA + подпись «Перезвоним в течение дня», «Номер увидит только тренер» — снимает главный страх «начнётся спам» |
| 5 | На мобильных CTA требовал прокрутки | **Липкая панель** «Позвонить / Бесплатная тренировка» внизу экрана (только мобильные/планшеты), чат-кнопка поднята выше панели, футер не перекрывается (спейсер + safe-area) |
| 6 | Чат-бот не предлагал мессенджер | Чип **«Написать в WhatsApp»** (зелёный) в fallback и отдельным правилом по ключевым словам («ватсап/телеграм/мессенджер») |
| 7 | Кнопка формы не отражала сценарий | Текст: **«Жду звонка тренера — записаться на бесплатную тренировку»**; поля: телефон обязателен, имя/email/локация/комментарий — по желанию (как заказано) |
| 8 | Демо сегодня | README: раздел «Куда попадают заявки» (с активацией FormSubmit за 1 клик) и **«Демо-чеклист»** из 5 пунктов; preconnect к formsubmit.co |

## Проверить владельцу (5 минут)

1. Отправить тестовую заявку → нажать «Activate» в письме от FormSubmit
   на `lubov.mir@mail.ru` (иначе письма не пойдут), проверить «Спам».
2. Убедиться, что WhatsApp привязан к номеру +7 963 670 06 40.
3. Отзывы: сейчас подготовленные тексты — по мере занятий заменять
   реальными (имена можно менять, факты — нет).

## Следующий шаг (этап 3 ROADMAP)

Яндекс.Метрика: цели «отправка формы», «клик по телефону», «WhatsApp»,
«открытие чата» — чтобы после старта рекламы видеть стоимость заявки.


---

# Аудит v4 (финальный) — 12.09.2026: глубокая проверка всего репозитория

Проверено: типы (tsc strict), обе сборки (base=/ и base=/voin-sveta/), все
пути к ассетам, YAML workflow, JSON-LD, синхронизация FAQ-текстов, якоря,
битые ссылки в dist, работа дев-сервера.

## Найдено и исправлено

| # | Ошибка | Критичность | Исправление |
|---|---|---|---|
| 1 | 12 абсолютных путей `/images/china/...` в JS/TSX (hero, аттестат, вся галерея): на GitHub Pages с base=/voin-sveta/ они вели бы в корень домена → **404 на все фото после деплоя** | 🔴 критическая | Хелпер `asset(path)` на `import.meta.env.BASE_URL` в content/Hero/Tradition; проверено в обеих сборках: `"/voin-sveta/images/..."` и `"/images/..."` компилируются корректно |
| 2 | og:image в index.html указывал на **удалённый** `images/hero.jpg` → пустое превью в соцсетях и 404 для краулеров | 🔴 критическая | Заменён на существующее `images/china/monks-demo.jpg` (абсолютный URL Pages) |
| 3 | FAQ-ответ в JSON-LD содержал **старую цену 4 500 ₽** (правка v2 не пережила сбой сессии) — расхождение видимого текста и SEO-разметки | 🟠 серьёзная | Обновлён; автоматическая сверка: совпадение с `PRICE_FAQ_ANSWER` дословно |
| 4 | Метка «· Telegram» у телефона сборов — Telegram в РФ заблокирован, метка вводила в заблуждение | 🟡 заметная | Заменено на «— телефон сборов» (Camp + README + комментарий в content.ts) |

| 25 | **Локальная сборка и CI считали `base` по-разному.** `package.json` держал захардкоженный `--base=/voinsveta/`, а workflow подставлял имя репозитория. Репозиторий теперь `voinsveta-ru/s` — то есть `npm run build:pages` собирал сайт с путями `/voinsveta/...`, а публикация ждала `/s/...`: ровно та поломка, которую чинили в v5 после прошлой смены владельца | 🔴 критическая | Один источник истины — `scripts/build-pages.cjs`: base берётся из `GITHUB_REPOSITORY` (CI) или `git remote origin` (локально), переопределяется `SITE_BASE`. Workflow теперь просто вызывает `npm run build:pages` |
| 26 | **Адрес сайта не согласован с репозиторием**: canonical, og:url, `sitemap.xml` и `robots.txt` указывают на `https://ruscli.github.io/voinsveta/`, репозиторий — `voinsveta-ru/s`, а GitHub Pages в нём не включены. Поисковикам отдаётся адрес, по которому сайта нет | 🟠 серьёзная | **Решено 12.09.2026 владельцем: боевой адрес — `https://voinsveta-ru.github.io/s/`.** Приведены все четыре места: `index.html` (canonical, og:url, og:image, `url` в JSON-LD организации), `public/sitemap.xml` (+ `lastmod`), `public/robots.txt` (Sitemap), base сборки (`scripts/build-pages.cjs` → `/s/`). `check.cjs` теперь сравнивает путь canonical с base сборки и предупреждает, чтобы рассогласование не вернулось при следующем переименовании |
| 27 | **`privacy.html`: «Вернуться на сайт» вела на `index.html`** — второй (неканонический) адрес той же страницы | 🟡 заметная | `href="./"` |
| 28 | **Сверка FAQ в `check.cjs` была хрупкой**: JSON-LD пишется руками, и при первом же `&nbsp;` в ответе проверка сообщала бы ложный «рассинхрон с content.ts» (в бандле лежит настоящий неразрывный пробел). Относительные локальные пути не проверялись вовсе | 🟡 заметная | Сущности декодируются перед сверкой; относительные пути проверяются на существование и дают предупреждение (на вложенном URL Pages они уезжают в несуществующий каталог) |

| 29 | **Деплой физически не мог опубликовать сайт.** `actions/configure-pages` с `enablement: true` создаёт Pages-сайт через `POST /repos/{owner}/{repo}/pages`, а этот эндпоинт требует `Pages: write` **и `Administration: write`** — последний `GITHUB_TOKEN` выдать нельзя (в блоке `permissions:` такого ключа нет). На репозитории, где Pages ни разу не включали, workflow падал с невнятным `Resource not accessible by integration`; README при этом утверждал, что «Pages включаются автоматически шагом configure-pages». Проверено на реальном прогоне после мержа PR №1 | 🔴 критическая (публикация) | Workflow первым шагом опрашивает `GET /repos/{owner}/{repo}/pages` и, если сайта нет, падает с понятной инструкцией (Settings → Pages → Source: GitHub Actions → перезапуск) вместо 403 из недр экшена; README исправлен: однократное включение руками — обязательный шаг |

## Проверено и подтверждено исправным

- tsc strict (noUnusedLocals/noUnusedParameters) — без ошибок; `npm run build` и `npm run build:pages` проходят
- Оба GitHub Actions workflow — YAML валиден (fetch-old-gallery уже выполнялся успешно)
- JSON-LD (Organization + FAQPage) — валидный JSON, цены/телефоны/тексты синхронизированы с видимыми блоками
- Все якорные ссылки ведут на существующие id; битых ссылок в dist/index.html нет (проверено скриптом с учётом base)
- В dist присутствуют 404.html и .nojekyll; sitemap.xml и robots.txt согласованы с адресом Pages
- WhatsApp-кнопки, отзывы, липкая панель, чат-бот — присутствуют в прод-сборке
- Дев-сервер отдаёт обновлённые модули, ассеты доступны

## Известные ограничения (не ошибки)

- Отзывы — подготовленные тексты (помечены в коде; заменяются реальными по мере поступления)
- Форма отправляет через FormSubmit; до нажатия «Activate» в почте школы письма не доставляются (WhatsApp/SMS-дубли работают всегда)
- Яндекс.Метрика ещё не подключена (следующий этап ROADMAP)

---

# Аудит v5 — 12.09.2026: переезд репозитория, мобильный UX, готовность к деплою

Репозиторий переехал: `swqmqwkhm9-byte/voin-sveta` → **`Ruscli/voinsveta`**.
Проверено: tsc strict, обе сборки, все URL/пути, workflow, живой дев-сервер
(все страницы и 10/10 фото — HTTP 200), якоря, JSON-LD, синхронизация FAQ.

## Найдено и исправлено

| # | Ошибка | Критичность | Исправление |
|---|---|---|---|
| 1 | Все публичные URL указывали на несуществующий сайт `swqmqwkhm9-byte.github.io/voin-sveta` (canonical, og:url, og:image, sitemap, robots): после деплоя — битые превью, неверная индексация | 🔴 критическая | Заменены на `https://ruscli.github.io/voinsveta/` в `index.html`, `public/sitemap.xml`, `public/robots.txt` |
| 2 | `build:pages` собирал с `--base=/voin-sveta/`, а workflow — с динамическим `/voinsveta/`: локальная Pages-сборка не совпадала с продом | 🔴 критическая | Base унифицирован: `/voinsveta/` (`package.json`, комментарий в `content.ts`, README) |
| 3 | Кнопка чата (`bottom-4`) перекрывалась липкой CTA-панелью на мобильных и сама закрывала кнопку «Бесплатная тренировка» | 🟠 серьёзная | FAB поднят над панелью на `<lg`: `bottom-[92px]`, на десктопе `lg:bottom-6`; панель чата уже была выше — проверена |
| 4 | В git лежал мусор `image-search/` (3.1 МБ, 5 файлов) — нигде не используется | 🟠 серьёзная | Удалён из репозитория (`git rm`) |
| 5 | GitHub Pages не был включён, API недоступно боту → деплой упёрся бы в ручной шаг | 🟠 серьёзная | В workflow добавлен `actions/configure-pages@v5` с `enablement: true` — Pages включается сам при первом деплое |
| 6 | В JSON-LD `EducationalOrganization` не было поля `url` | 🟡 заметная | Добавлен `url` на адрес Pages |
| 7 | У `og:image` не было размеров/alt — хуже превью в мессенджерах | 🟡 заметная | Добавлены `og:image:alt/width/height` |
| 8 | Заголовок галереи «Как проходят тренировки» + подводка про «атмосферу зала» не соответствовали реальным фото (поездки в Китай) | 🟡 заметная | Заголовок и подводка переписаны честно: «Школа в поездках: Шаолинь и Китай» |
| 9 | README: ссылка на несуществующий `public/images/hero.jpg`, старые URL, устаревшая инструкция включения Pages | 🟡 заметная | Актуализирован; ROADMAP 4.1 — новый URL |

## Проверено и подтверждено исправным

- `tsc` strict + `npm run build` + `npm run build:pages` — зелёные; в `dist` есть `404.html`, `.nojekyll`, `privacy.html`, `sitemap.xml`, `robots.txt`, все 10 фото
- Vite 8 корректно переписывает абсолютные пути `index.html` под base (`/voinsveta/...`) — ошибок 404 не будет
- JSON-LD (2 блока) — валидный JSON; FAQ-ответ про цену дословно совпадает с `PRICE_FAQ_ANSWER`
- Все 8 якорей меню + `#gallery`, `#reviews`, `#top` существуют; все внешние ссылки (`tel:`, `mailto:`, `wa.me`, `sms:`, карты) корректны
- Дев-сервер: корень, privacy, все фото — HTTP 200


---

# Аудит v6 — 12.09.2026: глубокая проверка поведения (не только типов и путей)

Предыдущие итерации проверяли типы, пути и контент. В этот раз сайт прогнали
через **поведенческие тесты**: сборка бандла + прогон в jsdom (68 проверок:
рендер, якоря, форма, FAQ-аккордеон, меню, галерея, лайтбокс, чат-бот,
32 живых вопроса боту), плюс проверка сгенерированного CSS (все 487 классов
Tailwind реально существуют) и обеих сборок (base=/ и base=/voinsveta/).
Ниже — только то, что ломалось на самом деле.

## Найдено → исправлено

| # | Ошибка | Критичность | Исправление |
|---|---|---|---|
| 1 | **Чат-бот отвечал невпопад.** Подбор ответа брал *первое* правило с совпадением и искал ключи простой подстрокой: «во сколько занятия?» → ответ про цену; «летние сборы» и «лагерь летом» → ответ про возраст; «благодарю!» → ответ про возраст (основа «год» находится внутри слова «бла-год-арю», а «лет» — внутри «билет»); «где вы находитесь?» → fallback (ключ «где наход» требовал соседства слов) | 🔴 критическая | Выбор ответа по **весу совпадения** (`findChatAnswer` в `content.ts`): основы ищутся только с начала слова, фразы из нескольких слов совпадают по всем словам независимо от порядка, побеждает правило с наибольшим суммарным весом. Приветствие/благодарность помечены `smallTalk` и срабатывают, только когда по существу вопроса ничего не найдено. Проверено на 32 репликах — все ответы по теме |
| 2 | **Форма заявки теряла введённые данные.** Клик по любому общему CTA (липкая «Бесплатная тренировка», «Записаться» в шапке/hero/ценах/отзывах) сбрасывал выбранную локацию и **затирал написанный комментарий**, если до этого родитель пришёл через «Записаться в эту группу» | 🔴 критическая | `open()` больше не подставляет `location: undefined / note: undefined`; форма применяет только то, что реально передали (`LeadForm` + `lead.tsx`) |
| 3 | **Повторный клик по той же кнопке игнорировался.** «Записаться в эту группу» → пользователь меняет локацию вручную → снова та же кнопка: страница прокручивалась, а форма не обновлялась (пропсы не менялись → эффект не запускался) | 🔴 критическая | В `LeadIntent` добавлен monotonic `token`: форма реагирует на каждый новый клик |
| 4 | **Результат отправки заявки выбрасывался.** `await deliverByEmail(form)` возвращал boolean, который никто не читал, а экран «Спасибо» утверждал «тренер Александр уже получил уведомление» — даже когда FormSubmit не ответил (адрес не активирован, нет сети, таймаут 8 с). Лид молча терялся | 🔴 критическая | Проверяется и `res.ok`, и тело ответа FormSubmit (`success`); при сбое доставки экран честно говорит «уведомление не ушло» и первым делом предлагает отправить заявку в WhatsApp одним касанием |
| 5 | **Hero на мобильных заезжал под шапку.** `pt-14` (56px) при высоте фиксированной шапки 72px — первый блок hero (чип «Шаолиньская традиция · набор 7–18 лет») перекрывался логотипом | 🔴 критическая | `pt-[92px] sm:pt-24` — отступ гарантированно больше шапки на всех брейкпоинтах |
| 6 | **Липкая панель и кнопка чата не учитывали safe-area.** На iPhone с «чёлкой» панель CTA выше на `env(safe-area-inset-bottom)` (~34px), поэтому FAB (`bottom: 92px`) наезжал на неё, а спейсер футера (76px) её не закрывал — низ футера был перекрыт | 🟠 серьёзная | Все три отсчёта переведены на `calc(... + env(safe-area-inset-bottom))` (`ChatBot.tsx`, `App.tsx`) |
| 7 | **Панель чата вылезала за верх экрана** в низком viewport (ландшафт телефона): `bottom: 156px` + `h: 66dvh` > высоты экрана | 🟠 серьёзная | Добавлен `max-h-[calc(100dvh-11.5rem)]` (+ вариант для `lg`) |
| 8 | **Ссылки на политику конфиденциальности были относительными** (`href="privacy.html"`): при заходе по вложенному пути (а `404.html` = `index.html`, то есть любой путь открывает сайт) ссылка вела в несуществующий каталог | 🟠 серьёзная | `asset("privacy.html")` в футере и в форме — путь всегда с base деплоя |
| 9 | **`og:image:width/height` = 1200×630, а файл `monks-demo.jpg` — 1100×619**: превью в мессенджерах кроилось/искажалось | 🟠 серьёзная | Размеры приведены к реальным, добавлен `og:image:type`; `check.cjs` теперь сверяет их с файлом автоматически |
| 10 | **Лайтбокс галереи не управлял фокусом**: фокус оставался на странице под модальным окном, Tab уходил на страницу, после закрытия фокус не возвращался на кнопку; блокировка прокрутки перевешивалась при каждом листании фото | 🟠 серьёзная | Фокус в диалог при открытии, возврат на кнопку-источник при закрытии, ловушка Tab, `aria-modal`; scroll-lock вынесен в отдельный эффект (`Gallery.tsx`) |
| 11 | **Чат-бот: Escape слушался на всём сайте постоянно** (даже закрытым), фокус не возвращался на кнопку, клик мимо панели не закрывал чат, массив таймеров рос бесконечно, ветка `action === "call"` в `handleChip` была недостижима (чипы «Позвонить» рендерятся ссылкой) | 🟠 серьёзная | Слушатели вешаются только на время открытия; возврат фокуса на FAB; закрытие по клику мимо; таймеры в `Set` с удалением после срабатывания; мёртвая ветка убрана |
| 12 | **`fetch-old-gallery.yml` сломался бы на втором запуске**: `git checkout -B photos/old-site` от main и `git push` в уже существующую ветку → non-fast-forward reject, фото перестали бы обновляться. Плюс workflow стартовал с любой ветки | 🟠 серьёзная | Ветка продолжается от своего же удалённого состояния (`fetch` + `checkout -B ... FETCH_HEAD`), запуск ограничен `main` |
| 13 | **`deploy-pages.yml`: `cancel-in-progress: true`** мог отменить идущий деплой Pages, оставив сайт без артефакта | 🟠 серьёзная | `false` (как в официальном шаблоне GitHub Pages) |
| 14 | **Контраст 18 фрагментов текста ниже WCAG AA** (`text-paper-100/40…/50` на графите = 3.2–4.5:1 при норме 4.5:1): подписи в футере, подсказки под полями формы, цена «вместо 4 900 ₽», подпись под аттестатом, дисклеймеры | 🟡 заметная | Поднято до `/60` (≈6:1), плейсхолдеры до `/55` (≈5.2:1); декоративный номер карточки (aria-hidden) — до `/55` |
| 15 | **Клавиатурная доступность шапки**: не было skip-link, у бургера не было `aria-controls`, меню не закрывалось по Escape и оставалось «открытым» в состоянии после ресайза до десктопа (шапка залипала залитой) | 🟡 заметная | Skip-link «Перейти к содержимому» + `id="main"`, `aria-controls`, закрытие по Escape и по `matchMedia(min-width: 64rem)` |
| 16 | **Закрытые вопросы FAQ оставались в дереве доступности** (панель схлопнута через `grid-rows-[0fr]`, но текст читался скринридером) | 🟡 заметная | `aria-controls` + `role="region"` + `aria-labelledby` + `aria-hidden` у закрытой панели |
| 17 | **Не было `color-scheme: dark`**: нативный `<select>` локации, скроллбары и автозаполнение браузера рисовались светлыми на тёмном фоне | 🟡 заметная | Добавлено в `@layer base` (в `privacy.html` такое уже было) |
| 18 | **CLS до загрузки изображений**: у hero-фото и аттестата не было `width`/`height` | 🟡 заметная | Размеры проставлены, hero получил `fetchPriority="high"` |
| 19 | **localStorage заявок рос бесконечно**, а повреждённая запись (не массив) ломала сохранение | 🟡 заметная | Храним последние 50; не-массив больше не роняет отправку |
| 20 | **Ошибки валидации не были связаны с полями** для скринридера | 🟡 заметная | `aria-describedby` на подсказку/ошибку + `role="alert"` у текста ошибки |
| 21 | **Мёртвый код**: `group/map` в `Schedule.tsx` без единого `group-hover/map:*` | 🟡 заметная | Удалено |
| 22 | **Документация расходилась с кодом**: README не перечислял `Gallery`, `Testimonials`, `StickyCta` и `scripts/check.cjs`; ROADMAP утверждал «заявки лежат в localStorage» и рекомендовал Telegram-бот (в РФ заблокирован — сам же AUDIT v3 это зафиксировал); пункт «добавить отзывы» числился несделанным; комментарий в `content.ts` про «мессенджеры блокируются» противоречил WhatsApp-кнопкам | 🟡 заметная | Всё приведено в соответствие |
| 23 | **`privacy.html` объявлял шрифт Manrope, но не подключал его** — страница выглядела чужой | 🟡 заметная | Подключён тот же Manrope |
| 24 | **Рассинхрон JSON-LD и `content.ts` нечем было поймать** — FAQ в `index.html` дублируется руками (в v4 уже раз расходился по цене) | 🟠 серьёзная | Новый `scripts/check.cjs` (`npm run check`) + шаг в CI: дословно сверяет вопросы/ответы FAQ из JSON-LD со строками в бандле, проверяет состав `dist` (`404.html` = `index.html`, `.nojekyll`), существование каждого локального пути из HTML и каждого ассета из JS/CSS, разрешение всех якорей, реальность размеров `og:image`, согласие `tel:`/`mailto:`/почты в JSON-LD с `src/content.ts`, связь `sitemap.xml`/`robots.txt`/`canonical`. Проверен на намеренно испорченной сборке — ловит все 4 подброшенные ошибки |

## Проверено и подтверждено исправным

- `tsc --strict` (+ `noUnusedLocals`/`noUnusedParameters`); в проверку типов
  добавлен `vite.config.ts` — зелёный
- `check.cjs` проверен на намеренно испорченной сборке: 7 подброшенных ошибок
  (размеры og:image, вопрос и ответ FAQ, битый абсолютный путь, битый
  относительный путь, несуществующий ассет в бандле, `404.html` ≠ `index.html`)
  — пойманы все 7, код возврата 1; на чистой сборке — 0
- `npm run build` и `npm run build:pages` — обе сборки проходят, `check.cjs`
  зелёный на обеих (base=/ и base=/voinsveta/)
- 68 поведенческих проверок в jsdom без единой ошибки консоли: рендер 14 секций,
  все id/якоря, src/alt у всех изображений, форматы `tel:`/`mailto:`/`sms:`/`wa.me`/
  карт, привязка `<label>` к полям, aria-метки у всех кнопок-иконок, аккордеон FAQ,
  мобильное меню, лайтбокс (←/→/Escape/блокировка прокрутки/возврат фокуса),
  чат-бот (чипы, отправка, закрытие), полный цикл формы (валидация → отправка →
  FormSubmit-тело → WhatsApp/SMS-дубли → localStorage → повторная заявка),
  преfill локации и комментария из CTA
- 487 классов Tailwind из TSX: каждый реально присутствует в собранном CSS
  (опечаток вида `text-paper-100/45` → несуществующий класс больше нет)
- `npm audit` — 0 уязвимостей; оба workflow — валидный YAML
- Дев-сервер: корень, `privacy.html`, favicon, фото, `robots.txt`, `sitemap.xml`
  и все модули — HTTP 200

Поведенческий harness писался под эту проверку и в репозиторий не кладётся
(jsdom не добавляли в зависимости — сайт намеренно лёгкий). Постоянная защита
от регрессий в CI — `scripts/check.cjs`: он не требует зависимостей и ловит
структурные/контентные ошибки (пути, якоря, JSON-LD, `og:image`, контакты).
- JSON-LD (Organization + FAQPage) — валидный JSON, 8 вопросов FAQ дословно
  совпадают с видимым блоком; все 10 фото галереи существуют и имеют alt

## Известные ограничения (не ошибки)

- **Pages нужно включить один раз руками** (Settings → Pages → Source:
  GitHub Actions) — см. п. 29. После этого публикация полностью автоматическая
- Отзывы — подготовленные тексты (помечены в коде, заменяются реальными)
- FormSubmit требует однократной активации адреса; до неё письмо не уходит —
  теперь это видно на экране «Спасибо» (п. 4), а WhatsApp/SMS-дубли работают всегда
- Яндекс.Метрика не подключена (этап 3 ROADMAP)
- `sms:` с предзаполненным текстом — кросс-платформенный хак `?&body=`:
  на части Android-устройств текст может не подставиться (пояснение в `content.ts`)


---

# Аудит v7 — 12.09.2026: повторный глубокий аудит после мержа PR №2 и включения Pages

Проведён перед первым боевым деплоем: полный проход по всем исходникам,
сборкам (base=/ и base=/s/), smoke-тест превью с правильным base,
поведенческий прогон чат-бота на 61 реплике, разбор аннотаций упавших
прогонов CI. Код после v6 в хорошем состоянии — найдено немного, но всё
реальное:

## Найдено → исправлено

| # | Ошибка | Критичность | Исправление |
|---|---|---|---|
| 1 | **Чат-бот: 7 из 48 тестовых реплик падали в fallback** из-за непокрытой русской морфологии: «когда/где проходят занятия?» (основа «заним-» не совпадает со словом «занятие»), «а девочек берёте?» («девочк» не покрывает «девочек»), «для дочки?» («дочке» не покрывает «дочки»), «нужна ли физподготовка» («подготовк» ищется только с начала слова, внутри «физ-подготовки» не найдётся), «мы новички» («новичок» ≠ начало «новичков/новичку»), фраза «с нуля» не могла сработать никогда (однобуквенное слово отсекается во взвешивании фраз) | 🟠 серьёзная | Основы переписаны под чередования: «заняти», «девоч», «доч», «нович», «физподготовк», «нуля»/«нулев». Чтобы «записаться на занятия» и «пробное занятие» не уезжали в правило расписания, в правило записи добавлены фразы «запис занят» и «пробн занят». Итог прогона: **61/61 реплика отвечает по теме** |
| 2 | **ChatBot: счётчик id сообщений инкрементировался внутри апдейтера состояния** — в React StrictMode апдейтер вызывается дважды, побочный эффект в нём даёт пропуски id (нечистая функция) | 🟡 заметная | `nextId.current++` вынесен из апдейтера, id считается до `setMessages` |
| 3 | **CI гасил Node 20 в каждом прогоне**: `actions/checkout@v4`, `configure-pages@v5`, `setup-node@v4`, `upload-pages-artifact@v3`, `deploy-pages@v4` — deprecated-предупреждение «forced to run on Node.js 24» в аннотациях каждого запуска | 🟡 заметная | Все экшены подняты до текущих мажоров на Node 24: `checkout@v7`, `configure-pages@v6`, `setup-node@v7`, `upload-pages-artifact@v5`, `deploy-pages@v5` (в обоих workflow; входы не менялись) |
| 4 | **`check.cjs`: проверка «canonical совпадает с og:url» была фикцией** — сверялось лишь наличие строки `href="..."` в HTML, само значение `og:url` не сравнивалось, рассинхрон прошёл бы незамеченным | 🟡 заметная | Настоящее сравнение `og:url` с canonical → ошибка блокирует публикацию |
| 5 | **DX-ловушка: `build:pages` + `npm run preview` открывали «сломанный» сайт** — preview берёт base `/` из `vite.config.ts` и не знает, с каким base собирался dist: любой запрос по `/s/…` отдаёт index.html через SPA-fallback, выглядит как полностью битая сборка | 🟡 заметная | Новый `scripts/preview-pages.cjs` (`npm run preview:pages`): запускает vite preview с тем же base, что и сборка; base вынесен в общий `scripts/resolve-base.cjs` (один источник истины для build и preview). Задокументировано в README |
| 6 | **Собственная регрессия (введена и поймана в этом же аудите):** при бампе версий экшенов шаг `deploy-pages` случайно оказался внутри job `build` — отдельный job `deploy` с `needs: build` и `environment: github-pages` пропал. Деплой при этом проходил, но терялись запись окружения и URL сайта в сводке прогона | 🟡 заметная | Каноничная двухджобовая структура восстановлена; проверена по `git show` и прогону |

## Проверено и подтверждено исправным

- Мерж PR №2 (понятная ошибка при выключенных Pages) — прогон после мержа
  упал ровно на новом шаге с инструкцией, а не «Resource not accessible»
- **GitHub Pages включены** (Settings → Pages → Source: GitHub Actions,
  `build_type: workflow`, https принудительно) — прогон деплоя после этого
  зелёный, сайт опубликован
- `tsc --noEmit`, `npm run build`, `npm run build:pages`, `npm run check` —
  зелёные, `check` без предупреждений (canonical `/s/` = base сборки `/s/`)
- Smoke-тест превью с base `/s/`: `/s/`, `/s/privacy.html`, `/s/sitemap.xml`,
  `/s/robots.txt`, `/s/favicon.svg`, `/s/images/china/monks-demo.jpg`,
  `/s/assets/*.{js,css}`, `/s/.nojekyll` — все 200 с правильными
  Content-Type; robots отдаёт `Sitemap: https://voinsveta-ru.github.io/s/sitemap.xml`
- Размеры всех 10 фото проверены; `width/height` в `Hero.tsx` (1100×619) и
  `Tradition.tsx` (1300×893) совпадают с реальными файлами
- Vite корректно переписывает `/favicon.svg` и `/images/…` в `index.html`
  под base `/s/` при сборке страниц
- Дубликатов id в разметке нет; все якоря (12, включая `#top`, `#gallery`,
  `#reviews`, `#contacts`) существуют; `tel:`/`mailto:`/JSON-LD согласованы
  с `content.ts`; FAQ в JSON-LD совпадает с бандлом дословно
- `fetch-old-gallery.yml`: механика ветки `photos/old-site` корректна,
  ограничение по paths исключает циклы

## Известные ограничения (не ошибки)

- Отзывы — подготовленные тексты (замена на реальные — по запросу)
- FormSubmit требует однократной активации `lubov.mir@mail.ru` (первое
  письмо с кнопкой Activate); до активации экран «Спасибо» честно предлагает
  WhatsApp/SMS
- Яндекс.Метрика — этап 3 ROADMAP
- `sms:` с предзаполненным текстом — хак `?&body=`: на части Android
  текст может не подставиться
- Мажорные теги экшенов (`@v7`, `@v6`, `@v5`) двигаются вместе с
  минорными релизами GitHub — при желании можно закатить на SHA
